@{
    ViewBag.Title = Model.Title;
}

@model NoteDto

<div class="form-floating mb-3">
    <a asp-controller="Home" asp-action="Home" class="btn btn-dark">Back</a>
</div>
<div class="card">
    <div class="card-body">
        <h5>@Model.Title</h5>
       <hr />
        <p>
            @Model.Content
        </p>
    </div>
</div>